<%= form_for(@card) do |f| %>
  <% if @card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@card.errors.count, "error") %> prohibited this card from being saved:</h2>

      <ul>
      <% @card.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <form role="form">
    <div class="form-group field">
      <label><%= f.label :serial %>
      <%= f.number_field :serial, :class => "form-control"  %></label>
    </div>
    <div class="form-group field">
      <label><%= f.label :name %>
      <%= f.text_field :name, :class => "form-control"  %></label>
    </div>
    <div class="form-group field">
      <label><%= f.label :zip %>
      <%= f.number_field :zip, :class => "form-control" %></label>
    </div>
    <div class="actions">
     <%= f.submit :class => "btn btn-default" %>
    </div>
  </form>
<% end %>
